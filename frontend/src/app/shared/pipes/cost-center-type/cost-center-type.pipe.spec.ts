import { CostCenterTypePipe } from './cost-center-type.pipe';

describe('CostCenterTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CostCenterTypePipe();
    expect(pipe).toBeTruthy();
  });
});
